import { AppImgFallbackDirective } from './app-img-fallback.directive';

describe('AppImgFallbackDirective', () => {
  it('should create an instance', () => {
    const directive = new AppImgFallbackDirective();
    expect(directive).toBeTruthy();
  });
});
